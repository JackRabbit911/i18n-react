# План реализации: i18n как сервис

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Перенести логику переводов из `TranslateProvider` (React-контекст) в фреймворк-агностичный plain-TS сервис с ленивым demand-driven фетчем, per-lang словарями, effector-зеркалом и тонкими React-адаптерами.

**Architecture:** Ядро — класс `I18nService` (`src/i18n/service.ts`), без React и без DOM (все зависимости — инъекция: transport, lang, delay, limit, preloadKeys). Конфиг (`config.ts`) собирает синглтон из текущих констант. React-адаптер — существующие хуки, переведённые на `useSyncExternalStore`. Effector-зеркало — `createI18nBridge` + дефолтные `$lang`/`$dict`, однонаправленный поток сервис → effector.

**Tech Stack:** TypeScript 5.9 (strict, `erasableSyntaxOnly`, `verbatimModuleSyntax`), React 19, effector 23, vitest 4 (новый devDependency), vite 8, express 5 (сервер не меняется).

**Spec:** `BIZ_REQs.md` (в корне репо; журнал прежнего ремонта — `remont.md`, его компромиссы §7.1/§7.2 этот план устраняет).

## Global Constraints

- Публичный контракт компонентов не меняется: хуки `useTranslate()`, `useSetLang()`, `useLangPrefix()` с прежними сигнатурами; потребители (Navbar, Page1–3, ModalContents, LangSwitcher, LangLink) не редактируются.
- Формат плейсхолдеров `%s`, плоские yaml-словари, endpoint `/api/gettranslate`, сервер (`src/server/*`) — без изменений.
- Новых runtime-зависимостей нет; `vitest` — только devDependency.
- tsconfig: `erasableSyntaxOnly` → никаких enum, namespace, параметр-свойств конструктора; `verbatimModuleSyntax` → типы импортировать через `import type`; `strict`, `noUnusedLocals`, `noUnusedParameters`.
- Тесты лежат рядом с кодом: `src/i18n/*.test.ts` (tsconfig.app включает `src` целиком; тесты импортируют модули **относительными** путями, чтобы не зависеть от алиасов).
- `service.ts` не должен читать `window`/DOM и не должен иметь top-level обращений к импортам из `config.ts`/`utils.ts` (в проекте уже есть рабочий цикл config↔utils; top-level чтений добавлять нельзя — иначе TDZ).
- Мерж словаря — copy-on-write (новый `Map` на каждый мерж): от этого зависят и effector-зеркало (сравнение ссылок), и семантика снапшотов.
- Конвенция коммитов репо: `#0001 <Type> - <описание>` + trailer `Co-Authored-By: Claude <noreply@anthropic.com>`.
- Верификация после каждого таска: `npx tsc -b` (0 ошибок) и `npm run lint` (0/0) перед коммитом.

---

### Task 1: Тест-инфраструктура — vitest + smoke-тест sprintf

**Files:**
- Create: `vitest.config.ts`
- Create: `src/i18n/utils.test.ts`
- Modify: `package.json` (script `test`)

**Interfaces:**
- Consumes: существующий `sprintf` из `src/i18n/utils.ts`.
- Produces: команда `npm test` (vitest run); последующие таски добавляют `*.test.ts` рядом с кодом.

- [ ] **Step 1: Установить vitest**

```bash
npm install -D vitest
```

- [ ] **Step 2: Добавить script в package.json**

В `scripts` добавить (рядом с `lint`):

```json
"test": "vitest run"
```

- [ ] **Step 3: Создать vitest.config.ts**

Отдельный конфиг (приоритет выше `vite.config.ts`) — тесты не тянут react/tailwind плагины:

```ts
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
})
```

- [ ] **Step 4: Написать smoke-тест sprintf**

```ts
// src/i18n/utils.test.ts
import { describe, expect, it } from 'vitest'

import { sprintf } from './utils'

describe('sprintf', () => {
  it('подставляет аргументы по плейсхолдерам %s', () => {
    expect(sprintf('Page %s of %s', 2, 3)).toBe('Page 2 of 3')
  })

  it('значение с % не перехватывает следующую подстановку', () => {
    expect(sprintf('(%s, %s)', '100%', 2)).toBe('(100%, 2)')
  })

  it('без аргументов плейсхолдер остаётся видимым', () => {
    expect(sprintf('Page %s')).toBe('Page %s')
  })
})
```

- [ ] **Step 5: Прогнать тесты**

Run: `npm test`
Expected: 3 passed. Если vitest не запускается — чинить инфраструктуру до continuing (следующие таски на ней стоят).

- [ ] **Step 6: Проверить, что tsc и eslint переваривают тест-файлы**

Run: `npx tsc -b && npm run lint`
Expected: 0 ошибок.

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json vitest.config.ts src/i18n/utils.test.ts
git commit -m "#0001 Add test infra - vitest + smoke-тест sprintf

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: Ядро сервиса — конвейер t() → pending → debounce → flush → merge → notify

**Files:**
- Create: `src/i18n/service.ts`
- Create: `src/i18n/service.test.ts`

**Interfaces:**
- Consumes: `sprintf` из `./utils`; типы `Argv`, `TranslateType` из `./types`.
- Produces (используют Tasks 3, 5, 6, 7): класс `I18nService`, типы `Transport = (lang: string, keys: string[]) => Promise<TranslateType>`, `I18nServiceOptions = { transport?, lang?, delay?, limit?, preloadKeys? }`. Публичные члены: `t(key, ...argv): string`, `subscribe(listener): () => void`, `getLang(): string`, `getVersion(): number`, `getDict(lang?): ReadonlyMap<string,string>`, `getPending(): string[]`. Все методы — стрелочные поля класса (авто-привязка + стабильные ссылки для `useSyncExternalStore`).

- [ ] **Step 1: Написать падающие тесты конвейера**

```ts
// src/i18n/service.test.ts
import { beforeEach, afterEach, describe, expect, it, vi } from 'vitest'

import { I18nService } from './service'
import type { TranslateType } from './types'

beforeEach(() => vi.useFakeTimers())
afterEach(() => vi.useRealTimers())

const makeTransport = (result?: TranslateType) =>
  vi.fn(async (_lang: string, keys: string[]) =>
    result ?? Object.fromEntries(keys.map(key => [key, key.toUpperCase()])))

describe('I18nService: конвейер', () => {
  it('t() без перевода возвращает фолбэк (sprintf ключа) и регистрирует ключ', () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 50 })

    expect(s.t('Hello %s', 'World')).toBe('Hello World')
    expect(s.getPending()).toEqual(['Hello %s'])
    expect(transport).not.toHaveBeenCalled() // debounce ещё не истёк
  })

  it('батчит ключи одного тика в один запрос после delay', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 50 })

    s.t('a'); s.t('b'); s.t('c')
    await vi.advanceTimersByTimeAsync(50)

    expect(transport).toHaveBeenCalledTimes(1)
    expect(transport).toHaveBeenCalledWith('en', ['a', 'b', 'c'])
    expect(s.t('a')).toBe('A')
    expect(s.t('b')).toBe('B')
    expect(s.t('c')).toBe('C')
  })

  it('повторный таймер не назначается, пока предыдущий не истёк', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 50 })

    s.t('a')
    await vi.advanceTimersByTimeAsync(30)
    s.t('b') // второй ключ в тот же pending-цикл
    await vi.advanceTimersByTimeAsync(50)

    expect(transport).toHaveBeenCalledTimes(1)
    expect(transport).toHaveBeenCalledWith('en', ['a', 'b'])
  })

  it('ключ, отсутствующий в ответе, получает значение = ключ (без вечного рефетча)', async () => {
    const transport = vi.fn(async () => ({ known: 'KNOWN' }))
    const s = new I18nService({ transport, lang: 'en', delay: 0 })

    expect(s.t('unknown')).toBe('unknown')
    await vi.advanceTimersByTimeAsync(0)

    expect(s.t('unknown')).toBe('unknown') // уже из словаря (self-fallback)
    expect(transport).toHaveBeenCalledTimes(1)
    expect(s.t('known')).toBe('KNOWN')
  })

  it('успешный мерж увеличивает version и уведомляет подписчиков', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 0 })
    const listener = vi.fn()
    s.subscribe(listener)

    const v0 = s.getVersion()
    s.t('a')
    await vi.advanceTimersByTimeAsync(0)

    expect(listener).toHaveBeenCalledTimes(1)
    expect(s.getVersion()).toBe(v0 + 1)
  })

  it('ошибка транспорта не роняет сервис: ключ можно запросить снова', async () => {
    const transport = vi.fn(async () => { throw new Error('boom') })
    const consoleError = vi.spyOn(console, 'error').mockImplementation(() => {})
    const s = new I18nService({ transport, lang: 'en', delay: 0 })

    s.t('a')
    await vi.advanceTimersByTimeAsync(0)
    expect(s.t('a')).toBe('a') // фолбэк

    transport.mockImplementation(async () => ({ a: 'A' }))
    s.t('a')
    await vi.advanceTimersByTimeAsync(0)
    expect(s.t('a')).toBe('A')
    consoleError.mockRestore()
  })
})
```

- [ ] **Step 2: Прогнать тесты, убедиться в падении**

Run: `npm test`
Expected: FAIL — `Cannot find module './service'` (или эквивалент).

- [ ] **Step 3: Написать реализацию service.ts**

```ts
// src/i18n/service.ts
import type { Argv, TranslateType } from "./types"
import { sprintf } from "./utils"

// транспорт — единственная внешняя зависимость ядра; инъекция делает
// сервис тестируемым в чистом node без DOM и без сети
export type Transport = (lang: string, keys: string[]) => Promise<TranslateType>

export type I18nServiceOptions = {
  transport?: Transport
  lang?: string
  delay?: number
  limit?: number | null
  preloadKeys?: readonly string[]
}

const EMPTY_DICT: ReadonlyMap<string, string> = new Map()

// фреймворк-агностичное ядро i18n: demand-driven сбор ключей, debounce-батчинг,
// per-lang словари с copy-on-write мержем, подписки для адаптеров (React/effector).
// Все методы — стрелочные поля: стабильные ссылки и привязка без .bind()
export class I18nService {
  private readonly transport: Transport
  private readonly delay: number
  private readonly limit: number | null

  private lang: string
  private dicts = new Map<string, ReadonlyMap<string, string>>()
  private pending = new Set<string>()
  private inFlight = new Map<string, Set<string>>() // lang -> ключи в запросе
  private listeners = new Set<() => void>()
  private version = 0
  private timer: ReturnType<typeof setTimeout> | null = null

  constructor(options: I18nServiceOptions = {}) {
    this.transport = options.transport ?? (async () => ({}))
    this.delay = options.delay ?? 0
    this.limit = options.limit ?? null
    this.lang = options.lang ?? ''
  }

  readonly t = (key: string, ...argv: Argv): string => {
    const value = this.dicts.get(this.lang)?.get(key)

    if (value !== undefined) {
      return sprintf(value, ...argv)
    }

    this.enqueue([key])
    // прогрессивный фолбэк: пока перевод едет, показываем ключ
    return sprintf(key, ...argv)
  }

  readonly subscribe = (listener: () => void): (() => void) => {
    this.listeners.add(listener)
    return () => {
      this.listeners.delete(listener)
    }
  }

  readonly getLang = (): string => this.lang

  readonly getVersion = (): number => this.version

  readonly getDict = (lang: string = this.lang): ReadonlyMap<string, string> =>
    this.dicts.get(lang) ?? EMPTY_DICT

  // интроспекция для тестов/отладки
  readonly getPending = (): string[] => [...this.pending]

  private enqueue(keys: Iterable<string>): void {
    const flight = this.inFlight.get(this.lang) ?? new Set<string>()
    this.inFlight.set(this.lang, flight)

    let added = false
    for (const key of keys) {
      if (flight.has(key) || this.pending.has(key)) continue
      this.pending.add(key)
      added = true
    }

    if (added) {
      this.schedule()
    }
  }

  private schedule(): void {
    if (this.timer !== null) return
    this.timer = setTimeout(() => {
      this.timer = null
      this.flush()
    }, this.delay)
  }

  private flush(): void {
    if (this.pending.size === 0) return

    const keys = [...this.pending]
    this.pending.clear()
    const lang = this.lang
    const flight = this.inFlight.get(lang) ?? new Set<string>()
    this.inFlight.set(lang, flight)
    keys.forEach(key => flight.add(key))

    this.transport(lang, keys)
      .then(result => this.merge(lang, keys, result))
      .catch(error => {
        console.error('i18n: fetch failed', error)
        // ключи снова доступны для постановки в очередь
        keys.forEach(key => flight.delete(key))
      })
  }

  private merge(lang: string, requested: string[], result: TranslateType): void {
    const flight = this.inFlight.get(lang)

    if (flight) {
      requested.forEach(key => flight.delete(key))
      if (flight.size === 0) {
        this.inFlight.delete(lang)
      }
    }

    // copy-on-write: новый Map на каждый мерж — адаптеры сравнивают ссылки
    const next = new Map(this.dicts.get(lang) ?? EMPTY_DICT)

    if (this.limit !== null) {
      const overflow = next.size + requested.length - this.limit
      if (overflow > 0) {
        [...next.keys()].slice(0, overflow).forEach(key => next.delete(key))
      }
    }

    // ключ, которого нет в ответе, получает значение = ключ:
    // инвариант «запрошен ⇒ в словаре» спасает от вечного рефетча
    requested.forEach(key => {
      next.set(key, Object.hasOwn(result, key) ? result[key] : key)
    })

    this.dicts.set(lang, next)

    // мерж в словарь не-текущего языка (поздний ответ при смене языка)
    // ложится в кэш тихо — без version bump и без notify
    if (lang === this.lang) {
      this.version += 1
      this.notify()
    }
  }

  private notify(): void {
    this.listeners.forEach(listener => listener())
  }
}
```

- [ ] **Step 4: Прогнать тесты**

Run: `npm test`
Expected: PASS — все тесты Task 2.

- [ ] **Step 5: Верификация tsc + eslint, commit**

Run: `npx tsc -b && npm run lint`
Expected: 0 ошибок.

```bash
git add src/i18n/service.ts src/i18n/service.test.ts
git commit -m "#0001 Add i18n service core - demand-driven конвейер: t/pending/debounce/merge/notify, copy-on-write словари

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: Ядро сервиса — языки: setLang, preloadKeys, изоляция гонки, кэш-хит

**Files:**
- Modify: `src/i18n/service.ts`
- Modify: `src/i18n/service.test.ts`

**Interfaces:**
- Consumes: класс `I18nService` из Task 2.
- Produces: новый член `setLang(lang: string): void` и опция `preloadKeys?: readonly string[]` в `I18nServiceOptions` (используют Tasks 5, 6, 7).

- [ ] **Step 1: Добавить падающие тесты в service.test.ts (новый describe)**

```ts
describe('I18nService: языки', () => {
  it('setLang уведомляет подписчиков сразу, словарь нового языка подтягивается конвейером', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 0, preloadKeys: [] })
    const listener = vi.fn()
    s.subscribe(listener)

    s.setLang('ru')
    expect(listener).toHaveBeenCalledTimes(1) // мгновенный notify
    expect(s.getLang()).toBe('ru')

    s.t('hello')
    await vi.advanceTimersByTimeAsync(0)
    expect(transport).toHaveBeenCalledWith('ru', ['hello'])
    expect(listener).toHaveBeenCalledTimes(2)
  })

  it('поздний ответ старого языка не уведомляет и не портит новый язык', async () => {
    let resolveEn!: (value: TranslateType) => void
    const transport = vi.fn((lang: string) =>
      lang === 'en'
        ? new Promise<TranslateType>(resolve => { resolveEn = resolve })
        : Promise.resolve({ hello: 'привет' }))
    const s = new I18nService({ transport, lang: 'en', delay: 0, preloadKeys: [] })
    const listener = vi.fn()
    s.subscribe(listener)

    s.t('hello')                          // en-запрос завис
    await vi.advanceTimersByTimeAsync(0)
    expect(listener).not.toHaveBeenCalled()

    s.setLang('ru')                       // notify №1
    s.t('hello')                          // ru-запрос — свой, per-lang
    await vi.advanceTimersByTimeAsync(0)
    expect(listener).toHaveBeenCalledTimes(2) // setLang + ru-мерж
    expect(s.t('hello')).toBe('привет')

    resolveEn({ hello: 'hello-en-late' }) // старый язык доехал поздно
    await vi.advanceTimersByTimeAsync(0)
    expect(listener).toHaveBeenCalledTimes(2) // без нового notify
    expect(s.t('hello')).toBe('привет')       // ru не загрязнён
    expect(s.getDict('en').get('hello')).toBe('hello-en-late') // лёг в кэш en
  })

  it('возврат на загруженный язык не ходит в сеть', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 0, preloadKeys: [] })

    s.setLang('ru')
    s.t('hello')
    await vi.advanceTimersByTimeAsync(0)
    const callsAfterFirstLoad = transport.mock.calls.length

    s.setLang('en')
    s.setLang('ru')
    expect(s.t('hello')).toBe('HELLO') // из кэша
    await vi.advanceTimersByTimeAsync(100)
    expect(transport.mock.calls.length).toBe(callsAfterFirstLoad)
  })

  it('preloadKeys фетчатся при старте и при каждой смене языка', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 0, preloadKeys: ['modalContent'] })

    await vi.advanceTimersByTimeAsync(0)
    expect(transport).toHaveBeenCalledWith('en', ['modalContent'])

    s.setLang('ru')
    await vi.advanceTimersByTimeAsync(0)
    expect(transport).toHaveBeenCalledWith('ru', ['modalContent'])
  })

  it('setLang на тот же язык — no-op без notify', () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', preloadKeys: [] })
    const listener = vi.fn()
    s.subscribe(listener)

    s.setLang('en')
    expect(listener).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: Прогнать тесты, убедиться в падении**

Run: `npm test`
Expected: FAIL — `s.setLang is not a function`, опция `preloadKeys` игнорируется.

- [ ] **Step 3: Дополнить реализацию**

В `I18nServiceOptions` добавить поле:

```ts
  preloadKeys?: readonly string[]
```

В класс добавить приватное поле (после `limit`):

```ts
  private readonly preloadKeys: readonly string[]
```

В конструктор (до `this.lang = ...`):

```ts
    this.preloadKeys = options.preloadKeys ?? []
```

В конец конструктора:

```ts
    this.enqueue(this.preloadKeys)
```

Добавить публичный метод (после `getPending`):

```ts
  // смена языка — тот же конвейер: мгновенный notify, словарь нового языка
  // подтягивается demand-driven фетчем (или мгновенно из кэша)
  readonly setLang = (lang: string): void => {
    if (lang === this.lang) return
    this.lang = lang
    this.version += 1
    this.notify()
    this.enqueue(this.preloadKeys)
  }
```

- [ ] **Step 4: Прогнать тесты**

Run: `npm test`
Expected: PASS — все тесты Tasks 2–3.

- [ ] **Step 5: Верификация tsc + eslint, commit**

Run: `npx tsc -b && npm run lint`
Expected: 0 ошибок.

```bash
git add src/i18n/service.ts src/i18n/service.test.ts
git commit -m "#0001 Add i18n service langs - setLang/preloadKeys, изоляция поздних ответов per-lang, кэш-хит без сети

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: Ядро сервиса — limit-эвикция (per-lang, как задокументированная возможность конфига)

**Files:**
- Modify: `src/i18n/service.test.ts`

**Interfaces:**
- Consumes: опция `limit` из Task 2 (реализация эвикции уже в `merge` — Task 2 включает её; этот таск только покрывает тестами и фиксирует семантику).
- Produces: тестовое покрытие опции `limit`.

- [ ] **Step 1: Добавить тесты (новый describe)**

```ts
describe('I18nService: limit', () => {
  it('limit выселяет самые старые ключи внутри языка', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 0, limit: 2, preloadKeys: [] })

    s.t('a'); s.t('b')
    await vi.advanceTimersByTimeAsync(0) // словарь {a, b}

    s.t('c')
    await vi.advanceTimersByTimeAsync(0) // overflow=1 → 'a' выселен

    expect(s.t('a')).toBe('a') // выпал → фолбэк на ключ (и повторная постановка)
    expect(s.t('b')).toBe('B')
    expect(s.t('c')).toBe('C')
  })

  it('limit=null (по умолчанию) ничего не выселяет', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 0, preloadKeys: [] })

    s.t('a'); s.t('b'); s.t('c')
    await vi.advanceTimersByTimeAsync(0)

    expect(s.t('a')).toBe('A')
    expect(s.t('b')).toBe('B')
    expect(s.t('c')).toBe('C')
  })

  it('эвикция не затрагивает словари других языков', async () => {
    const transport = makeTransport()
    const s = new I18nService({ transport, lang: 'en', delay: 0, limit: 1, preloadKeys: [] })

    s.t('a')
    await vi.advanceTimersByTimeAsync(0)
    s.setLang('ru')
    s.t('b')
    await vi.advanceTimersByTimeAsync(0)

    expect(s.getDict('en').get('a')).toBe('A') // en не тронут
    expect(s.getDict('ru').get('b')).toBe('B')
    expect(s.getDict('en').size).toBe(1)
    expect(s.getDict('ru').size).toBe(1)
  })
})
```

- [ ] **Step 2: Прогнать тесты**

Run: `npm test`
Expected: PASS (реализация из Task 2). Если какой-то тест падает — чинить реализацию `merge`, а не тест: семантика «старые-по-вставке выселяются первыми, эвикция per-lang» зафиксирована спекой (BIZ_REQs §4.3).

- [ ] **Step 3: Верификация tsc + eslint, commit**

Run: `npx tsc -b && npm run lint`
Expected: 0 ошибок.

```bash
git add src/i18n/service.test.ts
git commit -m "#0001 Add i18n service tests - limit-эвикция per-lang, семантика выселения по вставке

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: Сборка синглтона — отдельный модуль singleton.ts

**Files:**
- Create: `src/i18n/singleton.ts`

**Interfaces:**
- Consumes: `I18nService` + `I18nServiceOptions` из `./service`; константы `delay`, `limit`, `defaultTranslateKeys`, `getTranslate`, `detectLang` из `./config`.
- Produces (используют Tasks 6, 7): экспорт `i18n: I18nService` — синглтон, собранный из конфига.

- [ ] **Step 1: Создать singleton.ts**

```ts
// src/i18n/singleton.ts
import { I18nService } from "./service"
import { getTranslate, detectLang, delay, limit, defaultTranslateKeys } from "./config"

// синглтон i18n: единственное место, где ядро встречается с окружением
// (реальный транспорт, URL-детекция языка, debounce-задержка из конфига).
// Отдельный модуль — не config.ts: config участвует в цикле config↔utils
// (service → utils → config), и инстанциация класса из service на top-level
// config.ts ломала бы node-граф импортов (unit-тесты) TDZ-ошибкой.
export const i18n = new I18nService({
    transport: getTranslate,
    lang: detectLang(),
    delay,
    limit,
    preloadKeys: defaultTranslateKeys,
})
```

- [ ] **Step 2: Прогнать тесты — регрессии недопустимы**

Run: `npm test`
Expected: PASS (тесты импортируют service напрямую, не singleton; это проверяет, что ядро осталось чистым и новый модуль ничего не сломал на top-level).

- [ ] **Step 3: Верификация tsc + eslint, commit**

Run: `npx tsc -b && npm run lint`
Expected: 0 ошибок.

```bash
git add src/i18n/singleton.ts
git commit -m "#0001 Add i18n singleton - сборка I18nService из конфига в отдельном модуле singleton.ts

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: Effector-зеркало — createI18nBridge + дефолтные $lang/$dict

**Files:**
- Create: `src/i18n/effector.ts`
- Create: `src/i18n/effector.test.ts`

**Interfaces:**
- Consumes: `I18nService` (тип) из `./service` и синглтон `i18n` из `./singleton`.
- Produces: `createI18nBridge(service: I18nService): I18nBridge`, тип `I18nBridge = { $lang: Store<string>; $dict: Store<ReadonlyMap<string, string>> }`, дефолтные экспорты `$lang`, `$dict` над синглтоном. Поток данных строго сервис → effector.

- [ ] **Step 1: Написать падающие тесты зеркала**

```ts
// src/i18n/effector.test.ts
import { beforeEach, afterEach, describe, expect, it, vi } from 'vitest'

import { createI18nBridge } from './effector'
import { I18nService } from './service'
import type { TranslateType } from './types'

beforeEach(() => vi.useFakeTimers())
afterEach(() => vi.useRealTimers())

describe('effector-зеркало', () => {
  it('отражает смену языка и мерж словаря сервиса', async () => {
    const transport = vi.fn(async (_lang: string, keys: string[]) =>
      Object.fromEntries(keys.map(key => [key, key.toUpperCase()])))
    const service = new I18nService({ transport, lang: 'en', delay: 0, preloadKeys: [] })
    const { $lang, $dict } = createI18nBridge(service)

    expect($lang.getState()).toBe('en')

    service.setLang('ru')
    expect($lang.getState()).toBe('ru')
    expect($dict.getState().size).toBe(0) // словарь ru ещё пуст

    service.t('hello')
    await vi.advanceTimersByTimeAsync(0)
    expect($dict.getState().get('hello')).toBe('HELLO')
  })

  it('каждый мерж даёт новую ссылку словаря (copy-on-write доходит до стора)', async () => {
    const transport = vi.fn(async () => ({ a: 'A' }))
    const service = new I18nService({ transport, lang: 'en', delay: 0, preloadKeys: [] })
    const { $dict } = createI18nBridge(service)

    const before = $dict.getState()
    service.t('a')
    await vi.advanceTimersByTimeAsync(0)

    expect($dict.getState()).not.toBe(before)
    expect($dict.getState().get('a')).toBe('A')
  })

  it('зеркало однонаправленно: пишут только через сервис', async () => {
    const transport = vi.fn(async () => ({}))
    const service = new I18nService({ transport, lang: 'en', delay: 0, preloadKeys: [] })
    const { $lang } = createI18nBridge(service)

    // у сторов нет событий записи в сервис: смена языка только i18n.setLang
    service.setLang('ru')
    expect($lang.getState()).toBe('ru')
    expect(service.getLang()).toBe('ru')
  })
})
```

- [ ] **Step 2: Прогнать тесты, убедиться в падении**

Run: `npm test`
Expected: FAIL — `Cannot find module './effector'`.

- [ ] **Step 3: Написать реализацию effector.ts**

```ts
// src/i18n/effector.ts
import { createEvent, createStore } from "effector"
import type { Store } from "effector"

import { i18n } from "./singleton"
import type { I18nService } from "./service"

// read-only проекция сервиса в effector: поток данных строго сервис → effector.
// Назначение зеркала — реактивность (combine/sample в effector-сторах);
// чтение из не-React кода — прямой import { i18n } и вызов i18n.t()
export type I18nBridge = {
  $lang: Store<string>
  $dict: Store<ReadonlyMap<string, string>>
}

export const createI18nBridge = (service: I18nService): I18nBridge => {
  const langChanged = createEvent<string>()
  const dictChanged = createEvent<ReadonlyMap<string, string>>()

  const $lang = createStore(service.getLang())
  const $dict = createStore(service.getDict())

  $lang.on(langChanged, (_, lang) => lang)
  $dict.on(dictChanged, (_, dict) => dict)

  // ровно одна подписка на сервис гонит его изменения в effector;
  // effector сам дедуплицирует не изменившиеся значения сторов
  service.subscribe(() => {
    langChanged(service.getLang())
    dictChanged(service.getDict())
  })

  return { $lang, $dict }
}

// зеркало по умолчанию над сконфигурированным синглтоном
export const { $lang, $dict } = createI18nBridge(i18n)
```

- [ ] **Step 4: Прогнать тесты**

Run: `npm test`
Expected: PASS — все тесты Tasks 1–6.

- [ ] **Step 5: Верификация tsc + eslint, commit**

Run: `npx tsc -b && npm run lint`
Expected: 0 ошибок.

```bash
git add src/i18n/effector.ts src/i18n/effector.test.ts
git commit -m "#0001 Add effector bridge - однонаправленное зеркало \$lang/\$dict поверх I18nService, factory для тестов

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: React-адаптер — хуки на useSyncExternalStore, удаление провайдера

**Files:**
- Modify: `src/i18n/hooks.ts` (полная перезапись)
- Modify: `src/i18n/types.ts` (удалить `TranslateContextType`)
- Modify: `src/App.tsx` (убрать провайдер и `deps`)
- Delete: `src/i18n/context.ts`
- Delete: `src/i18n/TranslateProvider.tsx`
- Modify: `src/i18n/utils.ts` (удалить `updateTranslate` — мерж переехал в `service.merge`)
- Modify: `src/i18n/config.ts` (удалить неиспользуемый импорт `limit` из utils, если останется)

**Interfaces:**
- Consumes: синглтон `i18n` из `./singleton`; методы `t`, `subscribe`, `getLang`, `getVersion`, `setLang` из Task 3; `DEFAULT_LANG` из конфига.
- Produces: те же три хука с прежними сигнатурами — `useTranslate(): GetText`, `useSetLang(): SetLangHookType`, `useLangPrefix(): string`. Никто снаружи не меняется.

- [ ] **Step 1: Перезаписать hooks.ts**

```ts
// src/i18n/hooks.ts
import { useSyncExternalStore } from "react"

import { DEFAULT_LANG } from "./config"
import { i18n } from "./singleton"

import type { SetLangHookType } from "./types"

const subscribe = (listener: () => void) => i18n.subscribe(listener)
const getLang = () => i18n.getLang()
const getVersion = () => i18n.getVersion()

export const useTranslate = () => {
    // подписка перерисовывает только читателя при мерже словаря или смене языка
    useSyncExternalStore(subscribe, getVersion)
    return i18n.t
}

export const useSetLang = (): SetLangHookType => {
    const lang = useSyncExternalStore(subscribe, getLang)

    const setLang = (next: string) => {
        document.querySelector('html')?.setAttribute('lang', next)
        // очистка translate не нужна: сервис сам подтянет словарь нового языка
        i18n.setLang(next)
    }

    return [lang, setLang]
}

export const useLangPrefix = () => {
    const lang = useSyncExternalStore(subscribe, getLang)

    return (lang === DEFAULT_LANG) ? '' : lang
}
```

- [ ] **Step 2: Удалить контекст и провайдер**

```bash
git rm src/i18n/context.ts src/i18n/TranslateProvider.tsx
```

- [ ] **Step 3: Почистить types.ts — удалить TranslateContextType**

Из `src/i18n/types.ts` удалить блок (остальные типы — `Argv`, `GetText`, `TranslateType`, `SetLangHookType` — остаются):

```ts
export type TranslateContextType = {
  gettext: GetText;
  setTranslate: React.Dispatch<React.SetStateAction<TranslateType>>;
  lang: string;
  setLang: React.Dispatch<React.SetStateAction<string>>;
}
```

- [ ] **Step 4: Упростить App.tsx**

```tsx
// src/App.tsx
import Layout from "Layout"
import Router from "Router"

function App() {
  return (
    <Layout>
      <Router />
    </Layout>
  )
}

export default App
```

- [ ] **Step 5: Удалить updateTranslate из utils.ts**

Удалить из `src/i18n/utils.ts` функцию `updateTranslate` целиком (мерж с copy-on-write теперь в `service.merge`). Импорт `defaultTranslateKeys, getTranslateUri, limit` из config сократить до реально используемых: после удаления останутся `DEFAULT_LANG, SUPPORTED_LANGS, getTranslateUri` (проверить фактическое использование в файле перед правкой; `limit` больше из utils не используется). Если `grep -rn "updateTranslate" src/` находит ещё потребителей — остановиться и пересмотреть (не должно: единственный был TranslateProvider).

- [ ] **Step 6: Верификация tsc + eslint + build**

Run: `npm run build`
Expected: `tsc -b` 0 ошибок, `vite build` ✓. ESLint: `npm run lint` → 0/0 (в том числе правило react-refresh больше не имеет провоцирующих файлов).

- [ ] **Step 7: Прогнать все тесты**

Run: `npm test`
Expected: PASS (React-слой тестами не покрыт — jsdom/testing-library сознательно не добавляются, поведение хуков верифицируется живым запуском в Task 8; так зафиксировано в BIZ_REQs §5).

- [ ] **Step 8: Commit**

```bash
git add -A
git commit -m "#0001 Refactor react adapter - хуки на useSyncExternalStore поверх i18n-синглтона, удаление TranslateProvider/context/deps

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 8: Финальная верификация + статус в BIZ_REQs.md

**Files:**
- Modify: `BIZ_REQs.md` (строка «Статус»)

**Interfaces:**
- Consumes: всё предыдущее.
- Produces: подтверждённая сборка, тесты, живой прогон; обновлённый статус спеки.

- [ ] **Step 1: Полный статический прогон**

Run: `npm run lint && npx tsc -b && npm test && npm run build`
Expected: lint 0/0, tsc 0 ошибок, все тесты PASS, build ✓.

- [ ] **Step 2: Живой запуск сервера**

Run (в фоне):

```bash
npm run build && node src/server/server.js
```

- [ ] **Step 3: Smoke-проверки API и SPA-fallback (сервер не менялся — регрессия должна быть нулевой)**

```bash
curl -s -X POST http://localhost:8080/api/gettranslate \
  -H 'Content-Type: application/json' -H 'Accept-Language: ru' \
  -d '{"filter":["Homepage","Page%s"]}'
# Expected: {"success":true,"result":{"Homepage":"Главная","Page%s":"Страница%s"}} (точные значения — сверить с src/server/data/ru.yaml)

curl -s -o /dev/null -w '%{http_code}' http://localhost:8080/ru/page/2
# Expected: 200 (SPA-fallback)

curl -s http://localhost:8080/api/gettranslate -H 'Accept-Language: fr'
# Expected: результат en-словаря (фолбэк DEFAULT_LANG)
```

- [ ] **Step 4: Браузерная проверка (чек-лист для человека или headless)**

Открыть `http://localhost:8080`:
1. Главная на en рендерится с переводами (не сырыми ключами).
2. Переключить язык на ru: интерфейс переключается; в Network — один запрос `/api/gettranslate` c языком ru.
3. Вернуть en, снова включить ru: **повторного запроса нет** (кэш per-lang, BIZ_REQs §5).
4. Открыть modal: `modalContent`/`use` переведены (preloadKeys).
5. Навигация Page1→Page2→Page3, hard refresh вложенной страницы: переводы и маршруты работают.
6. React Profiler (если доступен): при мерже словаря ререндерятся только компоненты с `useTranslate`, не всё дерево.

- [ ] **Step 5: Обновить статус в BIZ_REQs.md**

Строку `**Статус:** дизайн утверждён в диалоге, реализация не начата` заменить на:

```markdown
**Статус:** реализовано (план: `docs/superpowers/plans/2026-08-30-i18n-service.md`)
```

- [ ] **Step 6: Остановить сервер, финальный commit**

```bash
git add BIZ_REQs.md
git commit -m "#0001 Update docs - BIZ_REQs.md: статус — реализовано

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Заметки для исполнителя

- **Цикл импортов config↔utils** существует и до этого плана. Он безопасен, пока ни один модуль цикла не инстанцирует классы на top-level. Поэтому синглтон живёт в отдельном `singleton.ts` (Task 5): node-граф тестов `service → utils → config` не тянет `service.ts` через config повторно, TDZ-ошибки «I18nService is not a constructor» не возникает. `config.ts` — только константы и функции; `new I18nService(...)` только в `singleton.ts`.
- **Семантическое отличие от старой системы** (осознанное, по спеке): `defaultTranslateKeys` теперь фетчатся при старте и при смене языка + по demand, а не пере-включались в каждый батч; словарь загруженного языка не очищается при уходе с него (кэш-хит при возврате).
- **ESLint-нюанс**: в тестах неиспользуемый первый аргумент транспорта именовать `_lang`/`_l` — правило `no-unused-vars` в конфиге репо не настроено на `argsIgnorePattern`, но аргумент до последнего используемого не флагается; при появлении ошибки — переименовать в используемый или прочитать значение.
- Если `npm install -D vitest` тянет версию, несовместимую с vite 8 (peer-предупреждения), зафиксировать последнюю совместимую и записать выбор в коммит-сообщение.
